APP_PORT=8080
# Use "localhost" for local dev (make run/dev), "postgres" for Docker (make docker-up)
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=secret
DB_NAME=backend_mp
# Use "disable" for local dev, "require" or "verify-full" for production
DB_SSL_MODE=disable
